export enum UserRole {
  user = 'user',
  manager = 'manager',
  admin = 'admin',
}

export enum Category {
  food = 'food',
  drinks = 'drinks',
  groceries = 'groceries',
  shopping = 'shopping',
  health = 'health',
  hotels = 'hotels',
  transport = 'transport',
  sites = 'sites',
}

export enum Accessibility {
  unknown = 0,
  non_compliant = 1,
  partially_compliant = 2,
  compliant = 3,
}
